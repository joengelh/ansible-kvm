---
- name: add ansible user and place ssh public key
  hosts: rootkvm
  roles:
  - user-add

- name: set hostname
  hosts: rootkvm
  roles: 
    - hostname-set

- name: place network scripts
  hosts: rootkvm
  roles: 
    - network
