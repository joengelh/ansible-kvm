---
- name: add ansible user and place ssh public key
  hosts: rootdev
  roles:
  - user-add

- name: set hostname
  hosts: dev
  become: yes
  roles: 
  - hostname-set

- name: place network scripts
  hosts: dev
  become: yes
  roles: 
  - network
