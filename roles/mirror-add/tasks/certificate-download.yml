---
- name: download certificate from master server
  copy:
    src: "{{ item }}"
    dest: /etc/pki/ca-trust/source/anchors/
  with_fileblob: "{{ netPath }}*"
  register: certCopy

- name: reload CA certificates
  command: update-ca-trust
  when: certCopy.changed
